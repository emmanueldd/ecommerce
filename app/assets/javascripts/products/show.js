App.products = App.products || {};
App.products.show = {
	init: function init() {
		self = this;
		// alert('products#show');
		// self.coucou('Emmanuel');
  },
	coucou: function coucou(name) {
		alert(name);
	}
}
